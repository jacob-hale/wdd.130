function changeText() {
    let newText = "hi there"
    let oldText = "Watch me whip"
    let pageText = document.getElementById('divWithText').innerHTML
    if (pageText == newText){
    document.getElementById('divWithText').innerHTML = oldText;}
    else
}